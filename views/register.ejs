<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Register</title>
        <script src="https://cdn.tailwindcss.com"></script>
        <link href="/css/styles.css" rel="stylesheet" />
    </head>
    <body
        class="bg-gradient-to-br from-indigo-500 to-purple-600 min-h-screen flex justify-center items-center p-4"
    >
        <div class="bg-white rounded-lg shadow-xl p-8 w-full max-w-md">
            <h1
                class="text-4xl sm:text-5xl font-main text-center text-indigo-600 mb-8"
            >
                Register
            </h1>
            <form id="register-form" class="space-y-6 font-second">
                <div>
                    <label
                        for="register-username"
                        class="block text-md font-bold text-gray-700 mb-1"
                        >Username</label
                    >
                    <input
                        id="register-username"
                        type="text"
                        placeholder="Choose a username"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 font-second"
                    />
                </div>
                <div>
                    <label
                        for="register-password"
                        class="block text-md font-bold text-gray-700 mb-1"
                        >Password</label
                    >
                    <input
                        id="register-password"
                        type="password"
                        placeholder="Create a password"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 font-second"
                    />
                </div>
                <div>
                    <label
                        for="confirm-password"
                        class="block text-md font-bold text-gray-700 mb-1"
                        >Confirm Password</label
                    >
                    <input
                        id="confirm-password"
                        type="password"
                        placeholder="Confirm your password"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500 font-second"
                    />
                </div>
                <button
                    type="submit"
                    class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 transition duration-300 font-second"
                >
                    Register
                </button>
            </form>
            <div
                id="message"
                class="mt-4 text-center text-md font-second"
            ></div>
            <p class="mt-6 text-center font-second">
                Already have an account?
                <a href="/auth/login" class="text-indigo-600 hover:underline"
                    >Login</a
                >
            </p>
            <a
                href="/"
                class="block mt-4 text-center text-indigo-600 hover:underline font-second"
                >Go back</a
            >
        </div>

        <script type="module" src="/js/auth.js"></script>
    </body>
</html>
